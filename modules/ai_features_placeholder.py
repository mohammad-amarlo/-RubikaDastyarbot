# ===================================================
# AI Placeholder – در نسخه‌ی بعدی به GAP‑AI متصل می‌شود
# ===================================================

def ai_response(prompt: str) -> str:
    """پاسخ مصنوعی ساده برای نمایش قابلیت AI Bridge"""
    default_answers = {
        "سلام": "درود بر تو 🌞",
        "ربات": "من دستیار روبیکا هستم، ساخت خالق من.",
        "کی هستی؟": "من Rubika Dastyarbot، نسخه آزمایشی v0.4‑alpha هستم.",
    }
    for k, v in default_answers.items():
        if k in prompt:
            return v
    return f"پرسش دریافت شد («{prompt}»)، ولی ماژول هوش هنوز روی پل GAP فعال نیست ⚙️"
