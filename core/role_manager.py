import json

# نقش‌ها با شناسه کاربر (در محیط واقعی از دیتابیس می‌خواند)
ROLES = {
    "mohammad_amarloo": "👑 مالک",
}

def get_role(user_guid: str) -> str:
    """برگشت نقش کاربر"""
    return ROLES.get(user_guid, "👤 کاربر عادی")
